apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jenks-pv-claim
  namespace: jenkins
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 9Gi
